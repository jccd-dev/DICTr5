//        $all_examinees_users = UsersData::paginate(15);

        /**
         * access it like this (example)
            @foreach ($myData as $data)
                <tr>
                <td>{{ $data->id }}</td>
                <td>{{ $data->name }}</td>
                <td>{{ $data->email }}</td>
                </tr>
            @endforeach

        *pagination links
            {{ $myData->links() }}
         */

//        return view('', ['examinees' => $all_examinees]);
